<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="StarterApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:StarterApp.Views"
    Shell.FlyoutBehavior="Disabled">

    <Shell.ToolbarItems>
        <ToolbarItem Text="Menu" Command="{Binding ShowAccountMenuCommand}" Order="Primary" Priority="0" />
    </Shell.ToolbarItems>

    <ShellContent Route="login" ContentTemplate="{DataTemplate views:LoginPage}" />
    <ShellContent Route="items" ContentTemplate="{DataTemplate views:ItemsListPage}" />
    <ShellContent Route="item" ContentTemplate="{DataTemplate views:ItemDetailPage}" />
    <ShellContent Route="rentals" ContentTemplate="{DataTemplate views:RentalsPage}" />
    <ShellContent Route="profile" ContentTemplate="{DataTemplate views:ProfilePage}" />
</Shell>
