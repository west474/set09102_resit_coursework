<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="StarterApp.Views.ItemsListPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:StarterApp.ViewModels"
             xmlns:models="clr-namespace:StarterApp.Database.Models;assembly=StarterApp.Database"
             x:DataType="viewmodels:ItemsViewModel"
             Title="{Binding Title}">

<!-- Generated with ChatGPT -->
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add Item" Command="{Binding AddItemCommand}" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto,Auto,*">

        <SearchBar Grid.Row="0"
                   Text="{Binding SearchText}"
                   SearchCommand="{Binding SearchCommand}"
                   Placeholder="Search for an item..."
                   Margin="10,5" />

        <Grid Grid.Row="1" Margin="10,5" ColumnDefinitions="Auto,*">
            <Label Grid.Column="0"
                   Text="Filter by Category:"
                   VerticalOptions="Center"
                   FontSize="16"
                   FontAttributes="Bold"
                   Margin="0,0,10,0" />

            <Picker Grid.Column="1"
                    ItemsSource="{Binding Categories}"
                    ItemDisplayBinding="{Binding Name}"
                    SelectedItem="{Binding SelectedCategory}"
                    FontSize="14" />
        </Grid>

        <RefreshView Grid.Row="2"
                     IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding RefreshCommand}">

            <CollectionView ItemsSource="{Binding Items}" SelectionMode="None">

                <CollectionView.EmptyView>
                    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
                        <Label Text="No items found" FontSize="18" TextColor="Gray" HorizontalOptions="Center" />
                        <Label Text="Try adjusting your search or filter" FontSize="14" TextColor="Gray"
                               HorizontalOptions="Center" Margin="0,5,0,0" />
                    </StackLayout>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Item">
                        <Grid Padding="15,10" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:ItemsViewModel}}, Path=EditItemCommand}"
                                    CommandParameter="{Binding .}" />
                            </Grid.GestureRecognizers>

                            <Label Grid.Row="0" Grid.Column="0" Text="{Binding Title}"
                                   FontSize="18" FontAttributes="Bold" />

                            <Label Grid.Row="0" Grid.Column="1"
                                   Text="{Binding DailyRate, StringFormat='£{0:F2}/day'}"
                                   FontSize="16" FontAttributes="Bold" TextColor="DodgerBlue" />

                            <Label Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Description}"
                                   FontSize="14" TextColor="Gray" LineBreakMode="TailTruncation" Margin="0,2,0,0" />

                            <Label Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding CategoryName}"
                                   FontSize="12" TextColor="Gray" Margin="0,2,0,0" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

        <ActivityIndicator Grid.Row="2" IsVisible="{Binding IsBusy}" IsRunning="{Binding IsBusy}"
                            Color="DodgerBlue" VerticalOptions="Center" HorizontalOptions="Center" />
    </Grid>
</ContentPage>